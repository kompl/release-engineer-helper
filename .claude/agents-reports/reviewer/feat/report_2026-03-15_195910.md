**Дата:** 2026-03-15 19:59
**Ветка:** feat
**Целевая ветка:** main

## Замечания

### [ВАЖНО] findStreakStart некорректно обрабатывает паттерны с чередованием ⚪ и 🔴

**Файл:** render/json.go, строки 372-380
**Проблема:** Алгоритм обратного обхода в `findStreakStart` проходит назад через все подряд идущие 🔴 и ⚪, а затем ищет первый 🔴 вперёд. Это приводит к тому, что паттерн `🟢⚪🔴🟢🔴` (не stable_failing, а flaky) будет ошибочно обработан: обратный обход пройдёт через `🔴🟢🔴` — нет, стоп, `🟢` остановит обход. Однако паттерн `🟢⚪🔴⚪🔴` будет обработан так: от конца назад через `🔴⚪🔴⚪` — все четыре позиции, idx=1, затем skip ⚪ вперёд до idx=2 (первый 🔴). Streak start указывает на orderedKeys[2], streak_length=2. Это корректно.

Но рассмотрим паттерн `🔴⚪🟢⚪🔴`. Обратный обход: positions[3]=⚪, проходим; positions[2]=🟢 — стоп, idx=3. Потом skip ⚪: positions[3]=⚪, idx=4 (🔴). Streak start = orderedKeys[4], streak_length=1. Корректно.

Однако проблема в другом: паттерн `⚪🔴⚪` — lastNonWhite=1 (🔴), обратный обход: idx от 2, positions[1]=🔴 — проходим, positions[0]=⚪ — проходим, idx=0. Skip ⚪: positions[0]=⚪, idx=1 (🔴). orderedKeys[1], streak_length=1. Но orderedKeys имеет длину 3 (по числу ранов), и idx=1 корректен. Ок.

После детального анализа: логика обратного обхода через ⚪ работает корректно для основных сценариев. **Снимаю замечание.**

---

### [ВАЖНО] Log fallback не очищает AllTestKeys, что приводит к некорректному presence detection

**Файл:** collect/collector.go, строки 243-253
**Проблема:** Когда artifact extraction возвращает `HasNoTests: true` и происходит fallback на логи (строка 244), поле `er.AllTestKeys` остаётся nil (из результата artExt.Extract). При сохранении в кэш (строка 257) `er.AllTestKeys` будет nil. Однако `er.Details` обновляется из logExt. Это значит, что для ранов, где данные получены из логов, AllTestKeys будет пустым. Это **ожидаемое поведение** — комментарий на строке 243 подтверждает это: "no all_test_names available from logs". Однако при повторной загрузке из кэша документ будет иметь Schema=3 с пустым AllTestKeys — и кэш вернёт его как валидный (Schema >= 3). В analyzeTestBehavior `namesForRun.Len() > 0` вернёт false, и состояние будет TestPassed — корректно. **Не баг.**

---

### [ВАЖНО] Бинарный файл bambai закоммичен/трекается в репозитории

**Файл:** bambai
**Проблема:** Файл `bambai` — это скомпилированный Mach-O 64-bit executable (arm64). Он трекается git-ом и входит в diff. Бинарные артефакты не должны находиться в системе контроля версий: они раздувают репозиторий, платформо-специфичны и пересобираются из исходников.
**Доказательство:** `git status` показывает `modified: bambai`, `file bambai` показывает `Mach-O 64-bit executable arm64`.
**Рекомендация:** Добавить `bambai` в `.gitignore` и удалить из трекинга: `git rm --cached bambai`.

---

### [ЗАМЕЧАНИЕ] Несогласованность именования поля AllTestKeys в cachedDocument

**Файл:** collect/cache.go, строка 30
**Проблема:** Поле структуры называется `AllTestKeys`, но bson-тег указывает `all_test_names`. Параметр функции `Save` тоже называется `allTestNames` (строка 98). Это создаёт путаницу — Go-код использует термин "keys", а хранилище и API — "names". Аналогично в `CacheEntry` (строка 67) поле называется `AllTestKeys`, а в `ExtractResult` (строка 29) и `processedRun` (строка 70 в collector.go) — тоже `AllTestKeys`, но с комментарием "base keys of ALL tests". При этом в `cachedDocument` bson-тег говорит "names". Это не баг, но может привести к путанице при отладке MongoDB-запросов (ожидаешь поле `all_test_keys`, а в документе `all_test_names`).
**Рекомендация:** Привести в соответствие — либо переименовать bson-тег в `all_test_keys`, либо Go-поле в `AllTestNames`.

---

### [ВАЖНО] Дедупликация allTestKeys отсутствует при агрегации из нескольких JUnit-файлов одного артифакта

**Файл:** collect/extractor_artifacts.go, строка 270 (в parseJUnitXML)
**Проблема:** В `parseJUnitXML` (строка 267-270) `allTestKeys` формируется как плоский список из `buildTestKey(tc)` для каждого testcase. Если в одном XML-файле есть два testcase с одинаковым classname::name (например, параметризованные тесты с одинаковыми classname и name, но разными параметрами, или retry-механизмы), то один и тот же ключ попадёт в список несколько раз. На уровне `parseJUnitZip` (строки 176-178) дедупликация происходит через `allKeysSet` (map), и на уровне `Extract` (строки 89-91) — тоже. Таким образом, дубликаты отфильтровываются на верхних уровнях. **Не баг, но лишние аллокации.**

---

### [КРИТИЧНО] Потенциальная ложная классификация stable_failing при определённых паттернах с TestNotPresent

**Файл:** analyze/analyzer.go, строки 167-170
**Проблема:** Рассмотрим паттерн (по present ранам): `🟢🔴🟢🔴`, но с ⚪ в середине: `🟢🔴⚪🟢🔴`. В `analyzeTestPattern`:
- lastPresentIdx = 4 (последний 🔴)
- lastFailIdx = 4
- lastPresentIdx == lastFailIdx -> true
- Вызывается `isStableFailingFrom(states, firstFailIdx=1)`.

`isStableFailingFrom` с startIdx=1 проверяет: states[1]=TestFailed (ok), states[2]=TestNotPresent (skip), states[3]=TestPassed -> return false. Значит behaviorType = "flaky". Корректно.

Теперь паттерн: `🟢🔴⚪🔴`.
- lastPresentIdx = 3
- lastFailIdx = 3
- lastPresentIdx == lastFailIdx -> true
- `isStableFailingFrom(states, firstFailIdx=1)`: states[1]=Failed (ok), states[2]=NotPresent (skip), states[3]=Failed (ok) -> return true.
- behaviorType = "stable_failing".

Но тест провалился только в 2 из 3 present ранов, и первый ран был зелёным. Это скорее flaky, чем stable_failing. Проблема в том, что `isStableFailingFrom` пропускает ⚪ раны и видит `🔴🔴` — сплошные неудачи от firstFailIdx до конца, игнорируя что до firstFailIdx был 🟢.

**Однако**: это поведение идентично оригинальной логике для паттерна `🟢🔴🔴` без ⚪ — он тоже классифицируется как stable_failing (все раны от firstFailIdx до конца — failed). Это не регрессия, а существующая семантика: "стабильно падает начиная с определённого коммита". Паттерн `🟢🔴🔴` означает именно это — тест начал падать и не перестал. **Не регрессия, существующая семантика.**

---

### [ВАЖНО] Log fallback сохраняет в кэш HasNoTests=true с Schema=3, блокируя будущий re-extract

**Файл:** collect/collector.go, строки 244-258
**Проблема:** Когда артефакты не содержат тестов (HasNoTests=true) и fallback на логи тоже не находит тестов (altHasNoTests=true или ошибка скачивания), в кэш сохраняется документ с Schema=3 и HasNoTests=true (строка 257). При следующем запуске `Cache.Load` (cache.go строка 86) проверяет `doc.Schema < 3 && !doc.HasNoTests` — для Schema=3 условие false, документ возвращается как cache hit. В `loadOrExtract` (collector.go строка 231) `entry.HasNoTests` = true, и вывод "Cache invalid for run ... (has_no_tests=true), re-extracting" срабатывает — код продолжает re-extract. **Корректно, не баг.**

Подождите, перечитаю:
```go
if found {
    if !entry.HasNoTests {
        fmt.Printf("  [collect] Cache hit for run %d\n", runID)
        return entry
    }
    fmt.Printf("  [collect] Cache invalid for run %d (has_no_tests=true), re-extracting\n", runID)
}
```

Да, при HasNoTests=true кэш hit отвергается и происходит re-extract. Это означает, что каждый запуск будет повторно пытаться скачать артефакты и логи для ранов без тестов. Это **по дизайну** (раны без тестов могут появиться из-за временных проблем, и повторная попытка оправдана), но потенциально неэффективно для ранов, которые объективно не содержат тестов (например, infrastructure-only workflow). Тем не менее, это не баг и не регрессия. **Не баг.**

---

### [ВАЖНО] findStreakStart может вернуть некорректный streak при паттерне с leading ⚪ перед streak

**Файл:** render/json.go, строки 372-380
**Проблема:** Рассмотрим паттерн `⚪⚪🔴🔴🔴`. Positions: [⚪, ⚪, 🔴, 🔴, 🔴]. lastNonWhite=4 (🔴). Обратный обход: idx от 4 назад через 🔴 и ⚪: positions[3]=🔴, positions[2]=🔴, positions[1]=⚪, positions[0]=⚪ — idx=0. Skip leading ⚪: positions[0]=⚪, positions[1]=⚪, idx=2 (🔴). orderedKeys[2], streak_length=3. Корректно.

Паттерн `🟢🔴⚪🔴🔴`. Positions: [🟢, 🔴, ⚪, 🔴, 🔴]. Обратный обход: positions[3]=🔴, positions[2]=⚪, positions[1]=🔴 — проходим (🔴 или ⚪), positions[0]=🟢 — стоп. idx=1. Skip ⚪: positions[1]=🔴 — не ⚪, остаётся idx=1. orderedKeys[1], streak_length: positions[1]=🔴(+1), positions[2]=⚪(skip), positions[3]=🔴(+1), positions[4]=🔴(+1) = 3. Корректно.

Паттерн `🟢⚪🔴🔴`. Обратный обход: positions[2]=🔴, positions[1]=⚪ — проходим, positions[0]=🟢 — стоп. idx=1. Skip ⚪: idx=2 (🔴). orderedKeys[2], streak_length=2. **Но здесь streak начался с orderedKeys[2], а не orderedKeys[1].** Ран orderedKeys[1] — ⚪ (тест отсутствовал). Streak start должен указывать на первый failing ран, что и происходит. Корректно.

**Снимаю замечание, алгоритм корректен.**

---

### [ЗАМЕЧАНИЕ] Закоммиченный бинарник bambai в git diff создаёт шум

Дублирует замечание выше про bambai. Убираю дубль.

---

## Итог

Найдено замечаний: 2 (критичных: 0, важных: 1, прочих: 1)

1. **[ВАЖНО]** Бинарный файл `bambai` трекается в репозитории — добавить в `.gitignore` и удалить из трекинга.
2. **[ЗАМЕЧАНИЕ]** Несогласованность именования `AllTestKeys` (Go) vs `all_test_names` (bson-тег) — может вызвать путаницу при отладке MongoDB.

Логика 3-state анализа (TestFailed/TestPassed/TestNotPresent) реализована корректно. Алгоритмы `isStableFailingFrom`, `hasFlakyBehavior`, `findStreakStart` правильно обрабатывают пропуски ⚪. Миграция кэша со Schema 2 на Schema 3 работает корректно — старые записи без AllTestKeys трактуются как cache miss. Fallback на логи корректно не заполняет AllTestKeys (данных для этого нет).
